<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@android:color/white"> <!-- 设置白色背景 -->

    <!-- 主要内容区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <!-- 用户头像和用户名 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"> <!-- 增加 gravity 保证垂直居中 -->

            <ImageView
                android:id="@+id/comment_user_avatar"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_marginEnd="10dp"
                android:src="@drawable/ic_launcher_foreground"
                android:background="@drawable/user_avatar_bg"
                android:scaleType="centerCrop" />

            <TextView
                android:id="@+id/comment_user_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Username"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="#333333" />
        </LinearLayout>

        <!-- 评论内容 -->
        <!-- 增加 marginStart 让文字和头像对齐，或者顶格，看你喜好。这里我移除了 marginStart 保持顶格 -->
        <TextView
            android:id="@+id/comment_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="这里是评论的内容"
            android:textSize="15sp"
            android:lineSpacingExtra="3dp"
            android:textColor="#000000" />

        <!-- 评论时间 -->
        <TextView
            android:id="@+id/comment_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1小时前"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="6dp"
            android:layout_gravity="start" /> <!-- 时间通常放在左边或者内容下方 -->

    </LinearLayout>

    <!-- 分割线 (Item Decorator 的替代方案，简单直接) -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:background="#E0E0E0" />

</LinearLayout>